apiVersion: v1
kind: Service
metadata:
  name: ts4u-portal
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ts4u-portal
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
